# <Name> Communication/Writing Style

<!-- Skeleton for a style directive. Every rule follows the template:
     RULE, TEST, ONE EXAMPLE PAIR. Sections marked (standard) exist
     because their absence is a known failure mode — adapt their content
     to the user, but do not drop them without a stated reason. -->

## Voice

<!-- The authoritative adjective list, first — highest-salience position.
     Then the prohibition list. One example pair anchoring the register
     boundary. No personas or archetypes: state the behaviors, not a
     figure who embodies them. -->

## Scope and precedence  (standard)

<!-- Where the style applies (deliverable types, correspondence, drafts
     written on the user's behalf). Carve-outs where external conventions
     win. Genre courtesies (greeting, thanks, sign-off) are structure,
     not sentiment. Then precedence: an explicit request for a different
     register overrides per piece; more specific instructions win over
     this default; and name the substance rules that yield to nothing
     (typically: no false or inflated claims, labeled uncertainty). -->

## Diction

<!-- Vocabulary range and deployment rule (complexity matches thought).
     Idiom/jargon policy with the precision test: terms that name
     well-defined things clarify; figurative jargon that gestures without
     fixing meaning is banned however common. The user's banned words and
     phrases belong here (and in the lint). -->

## Concision and formatting  (standard)

<!-- Length norms; when a minimal answer is the correct answer. Then
     formatting: prose vs lists (parallel independent items are
     enumerable; the steps of an argument are not), emphasis, headings,
     emojis. LLM formatting drift is the most visible style failure —
     be explicit here. -->

## Evidence and claims

<!-- What may be asserted and how confidence is expressed. For
     commercial/PR writing: the merit-claim test — a claim of merit must
     be specific and checkable; an adjective that cannot be replaced by
     a verifiable fact is cut. Include the labeling regime for
     estimates/projections rather than banning them. -->

## <Domain sections as needed>

<!-- e.g., Press releases; Client correspondence; Social copy. Only if
     the rules genuinely differ by deliverable — otherwise Scope covers
     it. -->

## Compact Instructions  (standard)

When compacting, state in the summary that these writing-style rules
remain in force, and preserve any style rulings made during the session.
